<!DOCTYPE html>
<html>
  <head>
    <title>Reservas - Consultorio Psicológico</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>

  <body>
    <div class="container mt-5">
      <div class="text-center mb-4">
        <h1 class="display-4 text-primary">Reservas</h1>
        <p class="lead text-muted">Lista de reservas realizadas en el consultorio.</p>
      </div>

      <!-- Tabla de reservas -->
      <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
          <h2 class="h5 mb-0">Listado de Reservas</h2>
        </div>
        <div class="card-body">
          <table class="table table-bordered table-striped table-hover">
            <thead class="thead-dark">
              <tr>
                <th>Nombre</th>
                <th>RUT</th>
                <th>Email</th>
                <th>Fecha y Hora</th>
              </tr>
            </thead>
            <tbody>
              <% if @reservas.present? %>
                <% @reservas.each do |reserva| %>
                  <tr>
                    <td><%= reserva.nombre %></td>
                    <td><%= reserva.rut %></td>
                    <td><%= reserva.email %></td>
                    <td><%= reserva.cita_disponible.fecha_hora.strftime("%d/%m/%Y %H:%M") %></td>
                  </tr>
                <% end %>
              <% else %>
                <tr>
                  <td colspan="5" class="text-center text-muted">No hay reservas registradas en este momento.</td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </body>
</html>
